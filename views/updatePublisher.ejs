<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>List of Publishers</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/publishers">List of Publishers</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/addPublisher">Add new publisher</a>
            </li> 
          </ul>
        </div>
      </div>
    </nav>
  <div class="container">
    <br>
    <div class="text-center"><h2>Update Publisher</h2></div>
    <br>
  
    <table class="table table-hover small text-center">
      <!-- <thead>
          <tr>
            <th width="100">Publisher Name</th>
            <th width = "100">Publisher Image</th>
            <th width="50">Country</th>
            <th width="50">Contact</th>
          </tr>
      </thead> -->
      <!--Added by me-->
      <% if (publishers) { %> 
            <form action="/updatePublisher/<%= publishers.publisher_id %>" method="POST" enctype = "multipart/form-data">
              <div class="mb-3">
              <label for="publisher_name" class="form-label">Publisher Name</label>
              <input type="text" class="form-control" id="publisher_name" name="publisher_name" value="<%= publishers.publisher_name%>" required>
            </div>

            <div class="mb-3">
              <label for="publisher_address" class="form-label">Publisher Address</label>
              <input type="text" class="form-control" id="publisher_address" name="publisher_address" value="<%= publishers.publisher_address %>" required>
            </div>

            <div class="mb-3">
              <label for="publisher_country" class="form-label">Country</label>
              <input type="text" class="form-control" id="publisher_country" name="publisher_country" value="<%= publishers.publisher_country %>" required>
            </div>

            <div class="mb-3">
              <label for="publisher_contact" class="form-label">Contact</label>
              <input type="text" class="form-control" id="publisher_contact" name="publisher_contact" value="<%= publishers.publisher_contact %>" required>
            </div>

            <div class="mb-3">
              <label class="form-label">Current Image</label>
              <input type="text" class="form-control" name="currentImages" value="<%= publishers.images %>" readonly>
              <img src="/images/<%= publishers.images %>" class="img-thumbnail mt-2" style="width: 150px;">
            </div>

            <div class="mb-4">
              <label for="images" class="form-label">Upload New Image</label>
              <input class="form-control" type="file" id="images" name="images" accept="images/*">
            </div>
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-primary">Update Publisher</button>
              <a href="/publishers" class="btn btn-secondary">Back to Publisher Page</a>
            </div>
          </table>
        </form>   
          <% } else { %>
              <p>No publisher found.</p>
          <% } %>
        </div>
    </div>
</body>
</html>
